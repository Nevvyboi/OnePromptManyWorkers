# Bind to every interface so phones on your hotspot can reach the laptop.
server.address=0.0.0.0
server.port=8080

# Let Server-Sent Events stay open indefinitely.
spring.mvc.async.request-timeout=-1

# false = real agents through Ollama (Qwen).  true = canned outputs, no model.
live.mock=false
live.model=qwen2.5:3b
live.ollama=http://localhost:11434
